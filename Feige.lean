import Feige.MainTheorem

/-!
# The unit-slack case of the sharp Feige theorem

Public entry point for the assembled formalization of the `δ = 1` case of
Theorem 1.1.  The exact calibration input, Theorem 2.1, is exposed separately
by `VlassisThomas`; the `α = 0` Grünbaum input used in §2.2 is exposed
separately by `Grunbaum`.
-/
